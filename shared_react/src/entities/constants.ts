export type LangC = {
  en: {
    subscribeNewsletter: LangSubscribeNewsletter;
  };
};

export type LangSubscribeNewsletter = {
  title: string;
  btnSubscribeNewsletter: string;
  placeholder: string;
};

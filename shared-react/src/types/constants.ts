export interface LangC {
  en: {
    subscribeNewsletter: LangSubscribeNewsletter;
  };
}

export interface LangSubscribeNewsletter {
  title: string;
  btnSubscribeNewsletter: string;
  placeholder: string;
}
